<UserControl x:Class="IMDB_final_Project.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IMDB_final_Project.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
   <Grid Background="#F0F0F0">
    <DockPanel>


        <!-- Main Content -->
        <StackPanel Margin="20" HorizontalAlignment="Center">
            <TextBlock Text="Search" Style="{StaticResource SectionHeader}" Margin="0,10"/>

            <!-- Filter and Search Row -->
            <StackPanel Orientation="Horizontal" Margin="0,10" HorizontalAlignment="Center">
                <ComboBox Width="150" Height="30" Margin="0,0,10,0">
                    <ComboBoxItem Content="All"/>
                    <ComboBoxItem Content="Movies"/>
                    <ComboBoxItem Content="Shows"/>
                    <ComboBoxItem Content="Actors"/>
                    <ComboBoxItem Content="Directors"/>
                </ComboBox>
                <TextBox Width="400" Height="30" Text="Search bar" Background="#888888" Foreground="White" Padding="5"/>
            </StackPanel>

            <!-- Search Results Area -->
            <Border Background="#666666" Margin="0,10" Padding="20" Width="600" Height="250">
                <TextBlock Text="Search results content" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </StackPanel>
    </DockPanel>
</Grid>

</UserControl>
